# 待辦清單（TODO）

> 依「現在就能做 / 需要前置作業 / 未來構想」分類。
> 完成的項目會移到 README 的開發路線圖。

最後更新：2026-09-04（完成第 1、2、3、4、5、6 項）

---

## 🟢 現在就能做（沒有前置阻礙）

### ~~1. drift schema migration 機制~~ ✅ 完成
已加上 `MigrationStrategy`（`onCreate` / `onUpgrade` 逐版處理 / `beforeOpen`），
並寫成 [資料庫與 Schema 變更指南](DATABASE.md)。
自動化 migration 測試仍待第 4 項（sqlite3 native lib）解決。

### ~~2. 餐點分餐別（早餐 / 午餐 / 晚餐 / 點心）~~ ✅ 完成
記錄時可選餐別（依時間自動預設）；今日分頁依餐別分組顯示並附小計；
統計分頁有各餐別熱量分佈與「這段期間吃最多的一餐」。
schema v1 時期的舊資料顯示為「未分類」。

### ~~3. 目標體重 + 預估達成時間~~ ✅ 完成
個人資料可設目標體重（選填）；體重分頁顯示還差多少、進度條、
依熱量赤字推估的達成週數與日期，趨勢圖上畫出目標線。
目標低於健康體重區間會出現勸阻提醒。「計算方式」也列出推估公式。

### ~~4. drift 整合測試（真實 SQL）~~ ✅ 完成
`test/drift_repository_test.dart` 用記憶體 SQLite 測真正的 SQL（當日 SUM、
日期邊界、intEnum、upsert、備份）；`test/drift_migration_test.dart` 建舊版
schema 的資料庫來驗證 v1 / v2 升級不會弄丟資料。
（原本記的「Windows 上 sqlite3 載不到」實際上不存在，直接就能跑。）

### ~~5. 匯出 CSV~~ ✅ 完成
備份頁多了「餐點 CSV」「體重 CSV」兩個匯出，格式依 RFC 4180 跳脫，
日期為 `yyyy-MM-dd`，畫面上明確標示 CSV 只能看、不能還原。

> 目前是複製到剪貼簿。之後若加上**檔案下載**，CSV 開頭要補 UTF-8 BOM
> （見 `lib/data/csv_export.dart` 的 `kUtf8Bom`），否則 Excel 開起來中文會亂碼。

### ~~6. 常用 / 最近吃過的食物快選~~ ✅ 完成
挑選食物頁最上面列出「最近吃過」（同名只留最近一次，最多 10 樣），
下面才是內建食物庫；搜尋會同時篩選兩邊。
帶入的是**自己那次調整過的數值**，比食物庫的通用估算更貼近實際。

### 7. 字串集中管理（i18n 準備）
中文字串目前散落在各畫面。抽到集中處，未來要改字或做多語系會好維護很多。

---

## 🟡 需要前置作業或你參與

### 8. README 加截圖
需要你從線上 Demo 截圖（今日 / 統計 / 體重 / 衛教），存到 `docs/screenshots/`，
我再排版進 README。

### 9. 拍照辨識（headline 功能）
需要：

1. 安裝 Android SDK（數 GB）
2. 一支實體 Android 手機（開 USB 偵錯）測相機
3. 整合手機端開源模型（中式食物的準確度是主要挑戰）

建議作法：先把「拍照 → 辨識介面 → 使用者確認份量 → 存成餐點」的流程打通，
模型放在**可抽換的介面**後面，之後再替換成更好的實作。

### 10. 提醒推播
需要 notification 套件與各平台設定；Web 端支援有限。

---

## 🔵 未來構想（暫不排程）

- Supabase 雲端同步（跨裝置）
- 情緒 / 壓力與飲食的關聯記錄
- 進度照片前後對比
- 家庭 / 共煮模式
- 商業化評估
